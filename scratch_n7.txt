N7 — Wciąż wysoka liczba `useFrame` callbacków (~35+)

**Opis:** Aktywne callbacki `useFrame` w każdej klatce:
- `GPUGeometry` — 3 rotacje wentylatorów
- `PSUGeometry` — 1 rotacja wentylatora  
- `FanGeometry` × 5 — 5 rotacji wentylatorów
- `CaseGeometry` — 3 lerpy (glass/side panels)
- `ComponentMesh` × 13 — position/scale lerp
- `CursorLight` — raycaster lerp
- `LocalAirflowParticles` × ~9 — forEach na cząsteczkach

**Wpływ:** Na słabszych procesorach wydłużony czas ramki JS.

**Naprawa:** Połącz rotacje fanów w jeden `useFrame` na poziomie `PCModel` z tablicą ref-ów.

---

### 🟠 N8 — Inline `meshStandardMaterial` z identycznymi parametrami tworzy duplikaty GPU

**Opis:** Parametry jak `color="#151515" roughness={0.6}` lub `color="#1a1a1a" roughness={0.4}` pojawiają się dziesiątki razy. R3F tworzy nową instancję `MeshStandardMaterial` dla każdego inline JSX.

**Wpływ:** ~80+ materiałów w pamięci GPU zamiast ~15 unikalnych.

**Naprawa:** Rozszerz [materials.ts](file:///d:/Projekty%20AI/PCVerse/src/components/PCModel/materials.ts) o brakujące materiały i użyj `material={materials.xxx}`.

---

### 🟠 N9 — `LocalAirflowParticles` — warunkowy `return null` odmontowuje InstancedMesh

**Opis:** [LocalAirflowParticles.tsx:63](file:///d:/Projekty%20AI/PCVerse/src/components/PCModel/geometries/LocalAirflowParticles.tsx#L63) — `if (!showAirflow) return null`. Toggle airflow powoduje pełny unmount/remount 9 instancji `InstancedMesh`.

**Wpływ:** Spowolnienie UI na ~100ms przy przełączaniu; geometria i materiały nie są reusowane.

**Naprawa:** Użyj `visible={showAirflow}` zamiast warunkowego renderowania.

---

### 🟡 N10 — 5 efektów post-processing na desktop, 0 na mobile — brak pośredniego trybu

**Opis:** [Scene3D.tsx:229-237](file:///d:/Projekty%20AI/PCVerse/src/components/Scene3D/Scene3D.tsx#L229-L237) — Bloom + N8AO + Vignette + ChromaticAberration + DepthOfField na desktop. `PerformanceMonitor` obniża tylko DPR, nie wyłącza efektów.

**Wpływ:** Na zintegrowanych GPU (Intel UHD) efekty mogą obniżyć FPS do 15-25.

**Naprawa:** W `PerformanceMonitor.onDecline` wyłącz najcięższe efekty (N8AO, DepthOfField).

---

### 🟡 N11 — `DepthOfField` toggle tworzy `<group />` zamiast warunkowego renderowania

**Opis:** [Scene3D.tsx:231](file:///d:/Projekty%20AI/PCVerse/src/components/Scene3D/Scene3D.tsx#L231) — `dofEnabled ? <DepthOfField ...> : <group /> as any`. Gdy DoF wyłączony, pusty element w EffectComposer.

**Wpływ:** Minimalny koszt, ale zła praktyka.

**Naprawa:** Użyj `{dofEnabled && <DepthOfField ... />}`.

---

## 3. UX/UI i Spójność Designu

### 🟠 N12 — Tooltip HTML z `<Html>` nie ma `occlude` — przebijanie się przez geometrię

**Opis:** [PCModel.tsx:151-218](file:///d:/Projekty%20AI/PCVerse/src/components/PCModel/PCModel.tsx#L151-L218) — dymki `<Html>` nie mają prop `occlude`. Dymki komponentów z tyłu mogą przebijać przez geometrię z przodu.

**Wpływ:** Wizualnie mylące w widoku eksplodowanym.

**Naprawa:** Dodaj `occlude` do `<Html>`.

---

### 🟡 N13 — Brak animacji wejścia tooltipów

**Opis:** Dymki pojawiają się natychmiastowo (React conditional rendering). Brak fade-in.

**Wpływ:** Mniej premium feel.

**Naprawa:** Owiń content w `<motion.div initial={{opacity:0}} animate={{opacity:1}}>`.

---

### 🟢 N14 — Flyout Environment/RGB — stała pozycja `left-12` na mobile

**Opis:** Na wąskich ekranach (< 360px) flyout może wypaść za prawą krawędź.

**Wpływ:** Na bardzo wąskich ekranach flyout niewidoczny.

**Naprawa:** Dodaj `max-w-[calc(100vw-3rem)]`.

---

## 4. Responsywność, Dostępność (WCAG) i SEO

### 🟠 N15 — Scena 3D ma `aria-hidden="true"` — brak alternatywnej nawigacji

**Opis:** [Scene3D.tsx:272](file:///d:/Projekty%20AI/PCVerse/src/components/Scene3D/Scene3D.tsx#L272) — Canvas div ukryty przed czytnikami ekranu. Brak alternatywnej listy komponentów.

**Wpływ:** Użytkownicy z assistive technology nie mają dostępu do głównej treści.

**Naprawa:** Dodaj ukrytą listę `<ul className="sr-only">` w `App.tsx` z linkami do komponentów.

---

### 🟠 N16 — Brak `aria-hidden="true"` na ikonach Lucide w toolbarze

**Opis:** Ikony Lucide w [UI.tsx](file:///d:/Projekty%20AI/PCVerse/src/components/UI/UI.tsx) nie mają `aria-hidden="true"`. Buttony mają `aria-label`, ale SVG generuje śmieciowy tekst.

**Wpływ:** Screen readery czytają nazwy SVG path-ów.

**Naprawa:** Dodaj `aria-hidden="true"` do każdej ikony.

---

### 🟡 N17 — Kontrast tekstu `text-slate-400` na ciemnym tle — poniżej WCAG AA

**Opis:** `text-slate-400` (#94a3b8) na `bg-[#0a0a0a]/90` ≈ kontrast 3.5:1. WCAG AA wymaga 4.5:1.

**Wpływ:** Tekst trudny do odczytania.

**Naprawa:** Zmień na `text-slate-300`.

---

### 🟡 N18 — WebGL error screen jest po angielsku

**Opis:** [App.tsx:31-33](file:///d:/Projekty%20AI/PCVerse/src/App.tsx#L31-L33) — "Your browser or device does not support WebGL..."

**Wpływ:** Niespójna lokalizacja.

**Naprawa:** Przetłumacz na polski.

---

### 🟢 N19 — ErrorBoundary: "Reload Application" po angielsku

**Opis:** [ErrorBoundary.tsx:36](file:///d:/Projekty%20AI/PCVerse/src/components/ErrorBoundary.tsx#L36) — button text.

**Wpływ:** Minor niespójna lokalizacja.

**Naprawa:** Zmień na "Odśwież stronę".

---

## 5. Błędy Logiczne, Edge Case'y i Potencjalne Crashe

### 🟠 N20 — `playAmbientSound()` — zombie `whineOsc` przy szybkim toggle

**Opis:** [audio.ts:83-113](file:///d:/Projekty%20AI/PCVerse/src/utils/audio.ts#L83-L113) — `stopAmbientSound()` ustawia `ambientOsc = null` natychmiast, ale `stop()` zaplanowane na `+1.1s`. Szybkie ON → OFF → ON tworzy nowy oscillator, ale stary `whineOsc` gra dalej.

**Wpływ:** Zombie oscillator, kumulacja przy szybkich toggleach.

**Naprawa:** Natychmiast `stop()` oscylatory przed ustawieniem na `null`.

---

### 🟡 N21 — `envMap` zdefiniowany po `SceneContent`

**Opis:** [Scene3D.tsx:256-261](file:///d:/Projekty%20AI/PCVerse/src/components/Scene3D/Scene3D.tsx#L256-L261) — `const envMap` po komponencie, który go używa. Działa dzięki hoistingowi, ale mylące.

**Wpływ:** Confusion przy code review.

**Naprawa:** Przenieś na górę pliku.

---

### 🟢 N22 — Camera intro — `requestAnimationFrame` zamiast smooth transition

**Opis:** [Scene3D.tsx:102-107](file:///d:/Projekty%20AI/PCVerse/src/components/Scene3D/Scene3D.tsx#L102-L107) — dwa zagnieżdżone rAF.

**Wpływ:** Na wolnych urządzeniach intro jako 2-klatkowy skok.

**Naprawa:** Użyj jednego `setLookAt` z `true`.

---

## 6. Optymalizacja Bundle Size i Ładowania Zasobów

### 🟠 N23 — 40 tekstur (3.14 MB) ładowane eagerly

**Opis:** 40 plików webp w `src/assets/` (3.14 MB). Preload obejmuje 7 kluczowych, ale reszta ładowana synchronicznie.

**Wpływ:** Na wolnym Wi-Fi (~2 Mbps) — ~12s na tekstury.

**Naprawa:**
1. KTX2 (Basis Universal) — 5-8× mniejsza.
2. 2-tier loading: krytyczne + lazy.
3. Redukcja rozdzielczości największych plików (`mobo_chipset.webp` 277KB, `case_back.webp` 276KB).

---

### 🟡 N24 — Pliki `scratch/` commitowane do repozytorium

**Opis:** ~15 skryptów Python w `scratch/` w repo.

**Wpływ:** Zanieczyszczenie repo.

**Naprawa:** `echo "scratch/" >> .gitignore && git rm -r --cached scratch/`

---

## 7. Jakość Animacji, Sterowania Kamerą i Interakcji

### 🟡 N25 — Hover lift offset niewidoczny na bardzo dużych komponentach

**Opis:** [PCModel.tsx:87-89](file:///d:/Projekty%20AI/PCVerse/src/components/PCModel/PCModel.tsx#L87-L89) — lift = 0.02 dla Case. Ledwie widoczny.

**Wpływ:** Brak feedback wizualny hover dla obudowy (ale może być zamierzone).

**Naprawa:** Opcjonalnie: dla `case` glow effect zamiast liftu.

---

### 🟢 N26 — `data.name.split(' - ')` powtarza się w 2 komponentach

**Opis:** Logika parsowania nazwy zduplikowana w [PCModel.tsx:188-196](file:///d:/Projekty%20AI/PCVerse/src/components/PCModel/PCModel.tsx#L188-L196) i [InfoPanel.tsx:167-174](file:///d:/Projekty%20AI/PCVerse/src/components/InfoPanel/InfoPanel.tsx#L167-L174).

**Wpływ:** Duplikacja logiki.

**Naprawa:** Wyeksportuj helper `splitComponentName()`.

---

### 🟢 N27 — `Bvh` brak `splitStrategy`

**Opis:** [PCModel.tsx:230](file:///d:/Projekty%20AI/PCVerse/src/components/PCModel/PCModel.tsx#L230) — `<Bvh firstHitOnly>` bez `splitStrategy="SAH"`.

**Wpływ:** Minimalny — BVH rebuild automatically.

**Naprawa:** Dodaj `splitStrategy="SAH"`.

---

### 🟢 N28 — Brak `pinch-to-zoom` weryfikacji na mobile

**Opis:** `CameraControls` z drei powinien obsługiwać touch natywnie. Warto zweryfikować.

**Wpływ:** Brak — CameraControls obsługuje touch.

**Naprawa:** Brak wymagana.

---

## Priorytetyzacja Działań

### Faza 1 — Krytyczne (tydzień 1)
1. **N5** — Dodaj `dispose()` do Shape/ExtrudeGeometry w CaseGeometry
2. **N6** — Przenieś tworzenie CanvasTexture w PSU poza komponent
3. **N20** — Fix zombie oscillator w ambient sound

### Faza 2 — High (tydzień 2-3)
4. **N7** — Zoptymalizuj useFrame (połącz rotacje fanów)
5. **N8** — Wyciągnij duplikowane materiały do `materials.ts`
6. **N9** — Zamień `return null` na `visible={false}` w LocalAirflowParticles
7. **N12** — Dodaj `occlude` do tooltipów HTML
8. **N15/N16** — Accessibility fixes (sr-only lista, aria-hidden na ikonach)
9. **N23** — Rozważ 2-tier loading lub KTX2 kompresję tekstur

### Faza 3 — Medium (tydzień 4+)
10. **N1** — Wyeksportuj wspólny `<Mesh>` wrapper
11. **N2** — Rozbij CaseGeometry na subkomponenty
12. **N10** — Redukcja efektów post-processing w PerformanceMonitor
13. **N13** — Animacja wejścia tooltipów
14. **N17/N18/N19** — Kontrast tekstu + tłumaczenia PL
15. **N21** — Przenieś `envMap` na górę pliku
16. **N24** — Dodaj `scratch/` do `.gitignore`
